# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = "django-insecure-cv9f_)dw1uujqq=1v7*+u$1mcx$+b!)+o93n01a@^ovydx(=fh"
# (substitua pela sua chave atual do settings.py, mas mantenha as aspas se a chave contiver caracteres especiais)

# Configurações de Banco de Dados (exemplo para PostgreSQL)
# Adapte para o seu banco de dados (PostgreSQL, MySQL, SQLite, etc.)
# Formato: postgres://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL='postgres://contratos_user:C0ntr4t0s_C0ntr0l@localhost:5432/contratos_db' 

# Se estiver usando SQLite localmente para desenvolvimento, pode ser:
# DATABASE_URL='sqlite:///db.sqlite3' 

# Configuração de Debug
DEBUG=True
# Mantenha True para desenvolvimento local, False para produção

# Hosts Permitidos (para desenvolvimento local)
ALLOWED_HOSTS=127.0.0.1,localhost 
# Em produção, será o seu domínio: ALLOWED_HOSTS=seusite.com,www.seusite.com